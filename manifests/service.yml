apiVersion: v1
kind: Service
metadata:
    name: "meteorweb-9253"
    labels:
        app: "meteorweb-9253"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "meteorweb-9253"